import { withLayout } from "../../components/Layout";
import { useEffect, useState } from "react";

const AdminResigner = () => {
  return <div>퇴사자 관리</div>;
};

export default withLayout(AdminResigner, { title: "퇴사자 관리" });
