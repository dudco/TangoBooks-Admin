import { withLayout } from "../../components/Layout";
import { useEffect, useState } from "react";

const AdminSalary = () => {
  return <div>급여 관리</div>;
};

export default withLayout(AdminSalary, { title: "급여 관리" });
